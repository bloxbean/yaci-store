---
title: API Reference
---

# 📋 API Reference

Welcome to the comprehensive REST API documentation for **Yaci Store v0.1.x**. This guide covers all available endpoints across different stores and modules.

## 🌐 Base Configuration

All API endpoints are served on the configured port (default: `8080`) with the base path structure:

```
http://localhost:8080/api/v1/{store}/{endpoint}
```

## 📊 Available Store APIs

### 🏗️ Block Store API

**Base Path**: `/api/v1/blocks`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/` | GET | Get all blocks | `GET /api/v1/blocks` |
| `/{blockNumber}` | GET | Get block by number | `GET /api/v1/blocks/1234567` |
| `/hash/{blockHash}` | GET | Get block by hash | `GET /api/v1/blocks/hash/abc123...` |
| `/latest` | GET | Get latest block | `GET /api/v1/blocks/latest` |
| `/epoch/{epochNumber}` | GET | Get blocks in epoch | `GET /api/v1/blocks/epoch/350` |

**Sample Response**:
```json
{
  "blockNumber": 1234567,
  "blockHash": "abc123def456...",
  "slot": 98765432,
  "epochNumber": 350,
  "epochSlot": 12345,
  "previousBlockHash": "def456abc123...",
  "issuerVkey": "pool1abc123...",
  "vrfKey": "vrf_vk1abc123...",
  "blockTime": 1640995200,
  "blockSize": 2048,
  "txCount": 15
}
```

---

### 💰 Transaction Store API

**Base Path**: `/api/v1/transactions`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/` | GET | Get all transactions | `GET /api/v1/transactions` |
| `/{txHash}` | GET | Get transaction by hash | `GET /api/v1/transactions/abc123...` |
| `/block/{blockNumber}` | GET | Get transactions in block | `GET /api/v1/transactions/block/1234567` |
| `/address/{address}` | GET | Get transactions for address | `GET /api/v1/transactions/address/addr1...` |

**Sample Response**:
```json
{
  "txHash": "abc123def456...",
  "blockNumber": 1234567,
  "blockHash": "def456abc123...",
  "slot": 98765432,
  "txIndex": 0,
  "fee": 170000,
  "size": 512,
  "validContract": true,
  "scriptDataHash": "script123...",
  "inputs": [...],
  "outputs": [...],
  "withdrawals": [...],
  "certificates": [...]
}
```

---

### 🗳️ UTxO Store API

**Base Path**: `/api/v1/utxos`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/` | GET | Get all UTxOs | `GET /api/v1/utxos` |
| `/address/{address}` | GET | Get UTxOs for address | `GET /api/v1/utxos/address/addr1...` |
| `/{txHash}/{outputIndex}` | GET | Get specific UTxO | `GET /api/v1/utxos/abc123.../0` |
| `/asset/{policyId}/{assetName}` | GET | Get UTxOs containing asset | `GET /api/v1/utxos/asset/policy123.../token` |

**Sample Response**:
```json
{
  "txHash": "abc123def456...",
  "outputIndex": 0,
  "address": "addr1qx2fxv2umyhttkxyxp8x0dlpdt3k6cwng5pxj3jhsydzer3n0d3vllmyqwsx5wktcd8cc3sq835lu7drv2xwl2wywfgse35a3x",
  "amount": [
    {
      "unit": "lovelace",
      "quantity": "1500000"
    },
    {
      "unit": "329728f73683fe04364631c27a7912538c116d802416ca1eaf2d7a96736174636f696e",
      "quantity": "100"
    }
  ],
  "dataHash": "datum123...",
  "inlineDatum": null,
  "referenceScriptHash": null,
  "slot": 98765432,
  "blockNumber": 1234567
}
```

---

### 🎨 Assets Store API

**Base Path**: `/api/v1/assets`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/` | GET | Get all assets | `GET /api/v1/assets` |
| `/{policyId}` | GET | Get assets by policy | `GET /api/v1/assets/policy123...` |
| `/{policyId}/{assetName}` | GET | Get specific asset | `GET /api/v1/assets/policy123.../token` |
| `/address/{address}` | GET | Get assets for address | `GET /api/v1/assets/address/addr1...` |
| `/metadata/{policyId}/{assetName}` | GET | Get asset metadata | `GET /api/v1/assets/metadata/policy123.../token` |

**Sample Response**:
```json
{
  "policyId": "329728f73683fe04364631c27a7912538c116d802416ca1eaf2d7a96",
  "assetName": "736174636f696e",
  "assetNameAscii": "satcoin",
  "fingerprint": "asset1cvmyrfrc7lpht2hcjwr9lulzyyjv27uxh3kcz0",
  "quantity": "1000000",
  "mintTxHash": "mint123def456...",
  "mintSlot": 98765000,
  "mintBlockNumber": 1234000,
  "metadata": {
    "name": "SatCoin",
    "description": "A test token",
    "image": "ipfs://QmHash...",
    "decimals": 6
  }
}
```

---

### 🏛️ Staking Store API

**Base Path**: `/api/v1/staking`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/pools` | GET | Get all stake pools | `GET /api/v1/staking/pools` |
| `/pools/{poolId}` | GET | Get stake pool details | `GET /api/v1/staking/pools/pool1...` |
| `/delegations/{stakeAddress}` | GET | Get delegations for stake address | `GET /api/v1/staking/delegations/stake1...` |
| `/rewards/{stakeAddress}` | GET | Get rewards for stake address | `GET /api/v1/staking/rewards/stake1...` |
| `/certificates` | GET | Get staking certificates | `GET /api/v1/staking/certificates` |

**Sample Response**:
```json
{
  "poolId": "pool1pu5jlj4q9w9jlxeu370a3c9myx47md5j5m2str0naunn2q3lkdy",
  "vrfKeyHash": "vrf_vk1abc123...",
  "pledge": "100000000000",
  "cost": "340000000",
  "margin": 0.05,
  "rewardAccount": "stake1u9ylzsgxaa6xctf4juup682ar3juj85n8tx3hthnljg47zctvm3rc",
  "poolOwners": ["stake1u9ylzsgxaa6xctf4juup682ar3juj85n8tx3hthnljg47zctvm3rc"],
  "relays": [...],
  "poolMetadataHash": "metadata123...",
  "activeEpoch": 350,
  "retireEpoch": null
}
```

---

### 📜 Script Store API

**Base Path**: `/api/v1/scripts`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/` | GET | Get all scripts | `GET /api/v1/scripts` |
| `/{scriptHash}` | GET | Get script by hash | `GET /api/v1/scripts/script123...` |
| `/native` | GET | Get native scripts | `GET /api/v1/scripts/native` |
| `/plutus` | GET | Get Plutus scripts | `GET /api/v1/scripts/plutus` |
| `/address/{address}` | GET | Get scripts for address | `GET /api/v1/scripts/address/addr1...` |

**Sample Response**:
```json
{
  "scriptHash": "67f33146617a5e61936081db3b2117cbf59bd2123748f58ac9678656",
  "type": "PlutusV2",
  "serializedSize": 1024,
  "json": {
    "type": "PlutusV2",
    "cborHex": "590400590300..."
  },
  "slot": 98765432,
  "txHash": "abc123def456..."
}
```

---

### 📅 Epoch Store API

**Base Path**: `/api/v1/epochs`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/` | GET | Get all epochs | `GET /api/v1/epochs` |
| `/{epochNumber}` | GET | Get epoch details | `GET /api/v1/epochs/350` |
| `/current` | GET | Get current epoch | `GET /api/v1/epochs/current` |
| `/{epochNumber}/params` | GET | Get epoch parameters | `GET /api/v1/epochs/350/params` |

**Sample Response**:
```json
{
  "epochNumber": 350,
  "startTime": 1640995200,
  "endTime": 1641427200,
  "firstSlot": 30240000,
  "lastSlot": 30671999,
  "blockCount": 21456,
  "txCount": 156789,
  "output": "15678900000000",
  "fees": "234567000000",
  "activeStake": "22500000000000000"
}
```

---


### 📝 Metadata Store API

**Base Path**: `/api/v1/metadata`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/` | GET | Get all metadata | `GET /api/v1/metadata` |
| `/tx/{txHash}` | GET | Get metadata for transaction | `GET /api/v1/metadata/tx/abc123...` |
| `/label/{label}` | GET | Get metadata by label | `GET /api/v1/metadata/label/721` |

---

### 🎁 MIR Store API

**Base Path**: `/api/v1/mir`

| Endpoint | Method | Description | Example |
|----------|--------|-------------|---------|
| `/certificates` | GET | Get MIR certificates | `GET /api/v1/mir/certificates` |
| `/certificates/{txHash}` | GET | Get MIR certificates in transaction | `GET /api/v1/mir/certificates/abc123...` |
| `/transfers` | GET | Get MIR transfers | `GET /api/v1/mir/transfers` |

---

For questions and support, join our [Discord Community](https://discord.gg/6AnKrFx4ne).