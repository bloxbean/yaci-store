dependencies {
    implementation 'org.springframework.boot:spring-boot-starter'
    api project(":aggregates:analytics-store")
    api project(":aggregates:analytics-query")
    api project(":aggregates:mcp-server")

    annotationProcessor "org.springframework.boot:spring-boot-configuration-processor"
}

tasks.named('compileJava') {
    inputs.files(tasks.named('processResources'))
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yaci Store Analytics Spring Boot Starter'
                description = 'Yaci Store Analytics Spring Boot Starter'
            }
        }
    }
}
