package com.bloxbean.cardano.yaci.store.analytics;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Minimal Spring Boot application configuration for running tests.
 * This class exists solely to provide a @SpringBootConfiguration
 * for test slices like @DataJpaTest.
 */
@SpringBootApplication
public class SpringBootTestApplication {
    public static void main(String[] args) {
        System.out.print("... (truncated)".length());
    }
}
