package com.bloxbean.cardano.yaci.store.mcp.server.dynamic.model;

/**
 * Supported JOIN types for multi-table queries.
 */
public enum JoinType {
    /** INNER JOIN */
    INNER,

    /** LEFT JOIN */
    LEFT,

    /** RIGHT JOIN */
    RIGHT
}
