snapshot-table:
  id: transaction-cbor
  spec-version: 1
  module: transaction
  kind: CHAIN_DATA
  restore: NOT_RESTORED
  reason: >-
    transaction CBOR has no analytics export; enable it only on an instance that syncs the range
    itself

  import:
    target-table: transaction_cbor
