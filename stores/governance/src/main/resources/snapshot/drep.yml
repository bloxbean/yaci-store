snapshot-table:
  id: drep
  spec-version: 1
  module: governance
  kind: CHAIN_DATA
  restore: IMPORT

  source:
    exporter-id: drep
    ducklake-relation: drep
    partition:
      strategy: DAILY
      column: block_time

  consistency:
    cutoff:
      type: SLOT_LTE
      column: slot

  import:
    target-table: drep
    mode: MAPPED
    columns:
      block_time:
        source: block_time
        converter: "timestamp-to-epoch-seconds"
    ignore-source-columns: [date]
    use-target-defaults: [update_datetime]
    batch-boundary: FILE_SET
    batch-size: 64

  validation:
    key: [drep_hash, tx_hash, cert_index, slot]
    bounds: [slot, epoch]
    required-columns: [drep_hash, tx_hash, cert_index, slot]
