dependencies {
    api project(":components:common")
    api project(":components:events")
    api project(":components:plugin")
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yaci Store Metadata'
                description = 'Yaci Store Metadata Module'
            }
        }
    }
}
