snapshot-table:
  id: transaction-metadata
  spec-version: 1
  module: metadata
  kind: CHAIN_DATA
  restore: IMPORT

  source:
    exporter-id: transaction_metadata
    ducklake-relation: transaction_metadata
    partition:
      strategy: DAILY
      column: block_time

  consistency:
    cutoff:
      type: SLOT_LTE
      column: slot

  import:
    target-table: transaction_metadata
    mode: MAPPED
    columns:
      block_time:
        source: block_time
        converter: "timestamp-to-epoch-seconds"
    ignore-source-columns: [date]
    batch-boundary: FILE_SET
    batch-size: 64

  validation:
    key: [id]
    bounds: [slot]
    required-columns: [id]
