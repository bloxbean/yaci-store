dependencies {
    api project(":components:common")
    api project(":components:events")
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yaci Store Assets'
                description = 'Yaci Store Assets Module'
            }
        }
    }
}
