dependencies {
    api project(":components:common")
    api project(":components:events")
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yaci Store MIR Module'
                description = 'Yaci Store MIR Module'
            }
        }
    }
}
