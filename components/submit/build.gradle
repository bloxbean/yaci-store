dependencies {
    api project(":components:common")
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yaci Store Sumbmit'
                description = 'Yaci Store Submit Module'
            }
        }
    }
}
