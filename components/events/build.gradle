dependencies {
    implementation project(":components")
    implementation 'com.bloxbean.cardano:yaci'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'yaci-indexer-events'
                description = 'Yaci Indexer Events'
            }
        }
    }
}
