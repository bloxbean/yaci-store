dependencies {
    api project(":components:common")
    api project(":components:vertx-int")

    api libs.graalvm.sdk
    api libs.graalvm.js
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yaci Store - Filter plugin module'
                description = 'Yaci Store - Filter plugin module'
            }
        }
    }
}
