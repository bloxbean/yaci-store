dependencies {
    api project(":components:common")
    api project(':components:events')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yaci Indexer Blocks'
                description = 'Yaci Indexer Block Processing Module'
            }
        }
    }
}
