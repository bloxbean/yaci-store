dependencies {
    api project(":stores:mir")

    implementation 'org.springframework.boot:spring-boot-starter-web'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                name = 'Yaci Store MIR API Module'
                description = 'Yaci Store MIR API Module'
            }
        }
    }
}
