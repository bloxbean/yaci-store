server:
  port: 8081

spring:
  banner:
    location: classpath:/banner.txt
  application:
    name: Yaci Store Aggregator App
  boot:
    admin:
      client:
        enabled: false

store:
  event-publisher-id: 2
  account:
    enabled: true
    balance-aggregation-enabled: true
    history-cleanup-enabled: false
    api-enabled: true
  aggr:
    utxo-storage-type: dummydb

apiPrefix: /api/v1

logging:
  file:
    name: ./logs/yaci-store-aggr.log
